# ── LangChain ecosystem ── pin tightly to avoid pipe-operator breakage
langchain==0.2.16
langchain-core==0.2.38
langchain-community==0.2.16
langchain-mistralai==0.1.13

# ── Mistral direct SDK (fallback if LangChain chain breaks) ──
mistralai>=1.0.0

# ── Streamlit ──
streamlit>=1.35.0

# ── Environment ──
python-dotenv>=1.0.0

# ── Async / HTTP ──
aiohttp>=3.9.0
requests>=2.31.0

# ── Data / utils ──
pandas>=2.0.0
pydantic>=2.5.0
tiktoken>=0.6.0
tenacity>=8.2.0
orjson>=3.9.0
rich>=13.7.0

# ── HTML parsing ──
lxml>=5.0.0
html5lib>=1.1
